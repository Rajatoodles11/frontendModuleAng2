// thi is default file run when no environment specify during app run

export const environment = {
  production: false,
  staging: false,
  development: false,
  default: true,
  loginApiUrl: 'http://example.com/api/',
};